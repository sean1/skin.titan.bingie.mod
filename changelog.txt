BINGIE Lite 2.3.12

- Reduce skin processing by removing inert textbox autoscroll work, redundant control groups and visibility constants, and duplicate conditional-animation state.
- Avoid rewriting an unchanged settings file while preserving startup cache refresh and migration behavior.

BINGIE Lite 2.3.11

- Avoid rebuilding widget item layouts when row focus changes while preserving focused Top 250 indicators.

BINGIE Lite 2.3.10

- Prevent list and task-pool workers from blocking when another worker drains the shared queue.
- Reduce navigator cleanup serialization and repeated landscape-card visibility work.
- Speed up IMDb and TVDB metadata cache lookups while preserving deterministic duplicate selection.
- Fold constant skin conditions across dialogs, views, and playback overlays without changing behavior.

BINGIE Lite 2.3.9

- Reduce trailer-preview polling overhead and repeated context checks, and batch database cleanup work.
- Remove unreachable skin controls and definitions while simplifying always-resolved include and variable conditions.
- Remove orphaned backgrounds and font data to shrink the install package by about 1 MiB.

BINGIE Lite 2.3.8

- Render Home content sooner and avoid unnecessary focused-metadata lookups.
- Wake Home content promptly while reducing idle background work.
- Remove unreachable seek-overlay controls without changing playback behavior.

BINGIE Lite 2.3.7

- Display pagination destinations as plain page numbers without decorative arrows.

BINGIE Lite 2.3.6

- Prefer a normal English audio track when available, then fall back to the title's original language.
- Recognize Kodi and TMDB language-code variants and preserve original-language metadata for movies and shows.
- Avoid commentary, descriptive-audio, and stale playback selections when switching tracks automatically.

BINGIE Lite 2.3.5

- Keep focused artwork visible until embedded trailer previews are ready, then crossfade cleanly.
- Stop active previews promptly when focus or navigation context changes.
- Reduce idle service polling after focused fanart is published.

BINGIE Lite 2.3.4

- Search high-yield torrent providers first and offer a full scrape when additional sources are available.
- Add an External Scrapers Manager for enabling or disabling individual providers.
- Coalesce DMM and Torz pack requests to prevent duplicate requests and cross-search result leakage.
- Filter candidates before debrid cache checks and close source-cache database connections deterministically.

BINGIE Lite 2.3.3

- Keep consecutive trailers attached to BINGIE when Kodi normalizes the local HLS URL.
- Preserve the embedded preview surface until the previous trailer has finished stopping.

BINGIE Lite 2.3.2

- Limit embedded trailer playback to a single 720p HLS video rendition and its matching audio.
- Resolve internal trailer URLs before playback so the final HLS stream cannot trigger a second fullscreen transition.
- Keep BINGIE's embedded preview surface selected for the complete trailer playback lifecycle.
- Return to the embedded surface if Kodi activates its fullscreen-video window.

BINGIE Lite 2.3.1

- Keep row previews and Extras trailers inside BINGIE's embedded video window instead of opening fullscreen.

BINGIE Lite 2.3.0

- Moved POV Lite browsing, metadata, search, source selection, playback, watched-state, Real-Debrid, subtitles, and service features into BINGIE.
- Added an internal trailer resolver so trailer playback no longer requires the YouTube add-on.
- Bundled the pure-Python networking and AES libraries used by the media runtime.
- Added safe one-time migration of existing POV Lite Real-Debrid settings and watched history.
- Added Real-Debrid authorization to BINGIE's settings screen.
